name: perf-enforced

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main, master]
  workflow_dispatch:

jobs:
  perf-enforced:
    uses: saagpatel/sovereign/.github/workflows/perf-enforced.yml@331b472c4ccebba09cf3f169e5fc6dca900ea571
